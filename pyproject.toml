[project]
name = "tempo-support-ingestion"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "psycopg[binary]>=3.2,<4",
  "requests>=2.32,<3",
]

[project.optional-dependencies]
test = ["pytest>=8,<9", "responses>=0.25,<1"]

[tool.pytest.ini_options]
pythonpath = ["."]
